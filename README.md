# Project Zoo Functions 🦁

Este projeto consiste na implementação de high order functions (HOFs) em **JavaScript** para gerenciar e consultar os dados de um zoológico a fim de praticar. O objetivo principal foi simular um sistema de gerenciamento de dados fictícios, aplicando lógica de programação e manipulação de objetos/arrays.

## 🛠️ Tecnologias Utilizadas

- **JavaScript (ES6+)**: Lógica principal e manipulação de dados.
- **Node.js**: Ambiente de execução.
- **ESLint**: Ferramenta de linting para padronização de código (padrão Trybe/Airbnb).

## 🚀 Funcionalidades Principais

O projeto contém diversas funções que operam sobre um conjunto de dados (`data/zoo_data.js`), incluindo:

- **Gestão de Animais**: Contagem e localização de animais (`countAnimals`, `getAnimalMap`).
- **Gestão de Colaboradores**: Consulta de quadro de funcionários e suas responsabilidades (`getEmployeesCoverage`, `getRelatedEmployees`).
- **Operacional**: Validação de horários de funcionamento (`getOpeningHours`) e cálculo de entradas (`calculateEntry`).
